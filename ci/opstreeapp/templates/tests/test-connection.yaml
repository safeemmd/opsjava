apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opstreeapp.fullname" . }}-test-connection"
  labels:
    {{- include "opstreeapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opstreeapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
