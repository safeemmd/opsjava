use employeedb;
create table Employee (empid INT AUTO_INCREMENT, empname varchar(150), empaddress varchar(200), salary long, empAge INT, primary key (empid));
create table Images (id INT AUTO_INCREMENT, imagename varchar(150), primary key (id));
